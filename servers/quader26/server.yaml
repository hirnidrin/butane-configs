# quader26 - homelab host: Flatcar, ZFS mirror, single-node k3s
#
# Build:  make quader26
#
# See README.md for provisioning, first-time pool creation and
# reprovisioning.
snippets:
  - base-core-user
  - base-hostname
  - net-static-ip-networkd
  - base-flatcar-updates
  - base-flatcar-no-docker
  - sysext-ipmitool
  - hw-ipmi-fans

# --- frame -------------------------------------------------------------------
# Merged last, so anything added here wins over the snippets above.
variant: flatcar
version: 1.1.0
