# Local environment files with secrets (any variant, but keep the committed example)
.env
**/.env
.env.*
!.env.example

# Generated files from templates
*.butane
*.ign

# Build staging (merged config + substituted payload files)
.build/
**/.build/

# Backup files
*.bak
*~

# Local tool state
.claude/settings.local.json
.superpowers/
